{
  "name": "gerenciador-de-tarefas",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "db:up": "docker compose up -d mongo mongo-setup redis",
    "dev": "concurrently \"npm run dev --workspace apps/api\" \"npm run dev --workspace apps/web\"",
    "lint": "npm run lint --workspaces",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky",
    "test": "npm run test --workspaces",
    "test:e2e:web": "npm run test:e2e:web:setup && npm run test:e2e --workspace apps/web",
    "test:e2e:web:setup": "npm run prisma:generate --workspace apps/api && npm run prisma:migrate --workspace apps/api && npm run seed --workspace apps/api"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "concurrently": "^9.1.2",
    "eslint": "^8.57.1",
    "eslint-plugin-react-hooks": "^4.6.2",
    "husky": "^9.1.7",
    "prettier": "^3.5.3"
  }
}
